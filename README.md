# boardgame-record

## commit convention

`@-###: [brief explanation]`

- `@`: commit scope (frontend: 'f', backend: 'b', entire project: 'e', etc.)
- `###`: representing word (add, init, del, mod, etc.)
